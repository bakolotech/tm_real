function sendNotificationOnStock() {

    $('#inviteRegisterLoginModal').modal({
        backdrop: 'static',
        keyboard: false
    })
    
    $('#inviteRegisterLoginModal').modal('show')
    // load__invite_resgister_login()
    console.log('Go notify me ...')
}